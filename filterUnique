Array.prototype.unique = function() {
    let arr = [];
    for(let i = 0; i < this.length; i++) {
        if(!arr.includes(this[i])) {
            arr.push(this[i]);
        }
    }
    return arr; 
  }
  
  const str = ["abb", "abc", "abcdb", "aea", "bbb"]
  const uniqueStr = str.unique()
  console.log(uniqueStr)
